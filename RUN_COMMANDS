# UI

```
cd /Users/sumangat/Desktop/CursorMeetup/AprilProject/web
cp .env.example .env.local        # set OPENAI_API_KEY (and JIRA_* if pushing)
npm install
npm run dev                       # http://localhost:3000
```


# Python

```
cd /Users/sumangat/Desktop/CursorMeetup/AprilProject
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env              # set OPENAI_API_KEY
 
python -m agent.main              # dry-run
python -m agent.main --push       # enable /push to Jira
python -m tests.test_smoke        # smoke tests
```
